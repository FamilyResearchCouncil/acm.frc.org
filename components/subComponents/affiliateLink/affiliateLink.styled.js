import styled from 'styled-components';

export const StyledAffiliateLink = styled.a`
  display: block;
  text-align: center;
  transition: all .2s ease;
  
  img {
    max-height: 90px;
  }
  &:hover {
    transform: scale(1.1);
  }

`;